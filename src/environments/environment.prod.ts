export const environment = {
  production: true,
  apiUrl: '/api',
  /**
   * Public site origin used by SeoService for canonical URLs and sitemap
   * references. Update this to the real production domain before deploying —
   * wrong values here leak `example.com` into Open Graph and search previews.
   */
  siteUrl: 'https://leowear.example.com'
};
